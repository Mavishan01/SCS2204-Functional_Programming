import Q8_01.encryption
import Q8_01.decryption
import Q8_01.cipher
import Q8_02.patternMatch

object Q07_Main{
    def main(args: Array[String]): Unit = {
        val text = "abc xyz"
        val jumps = 5

        // println(encryption(text, jumps))
        // println(decryption(text, jumps))

        // println(cipher(text, jumps, encryption, decryption))

        // patternMatch()
    }
}